# Changesets

This directory holds per-package version-bump intents. See
[changesets docs](https://github.com/changesets/changesets).

To add a changeset:

```bash
pnpm changeset
```

Then `pnpm version` consumes them to bump package versions.
